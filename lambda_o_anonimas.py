# Definir la función lambda para determinar si un número es par o impar
#es_par_o_impar = lambda x: "Par" if x % 2 == 0 else "Impar"

# Pedir al usuario que ingrese un número
#numero = int(input("Ingrese un número: "))

# Obtener el resultado utilizando la función lambda
#resultado = es_par_o_impar(numero)

# Imprimir el resultado
#print(f"El número ingresado es {resultado}")


#Ejemplo 
#calcular = lambda num: "esta aprobado" if num > 4 else "No esta aprobado"
#print(calcular(2))

#Ejemplo 2
# sumar = lambda x, y : x + y
# print(sumar(10,1))


#Ejemplo 1
#print((lambda num: "es par" if num % 2 == 0 else "No es es par")(2))
